import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#FFFFFF",
    },
    secondary: {
      main: "#1943ED",
    },
    third: {
        main: "#C7CDE8",
    },
  },
  typography: {
    fontFamily: "Arial",
  },
});

export default theme;
